#include <iostream>
#include <cmath>
int main() {
    int n;
    std::cin >> n;
    double sqrt_5 = std::sqrt(5.);
    double l = 0.5 + 0.5 * sqrt_5;
    double u = 0.5 - 0.5 * sqrt_5;
    std::cout << static_cast<long long>((std::pow(l, n + 2) - std::pow(u, n + 2))/sqrt_5);
    return 0;
}
